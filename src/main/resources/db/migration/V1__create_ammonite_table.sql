CREATE TABLE ammonite (
    id INT AUTO_INCREMENT PRIMARY KEY,
    taxonomy_subclass VARCHAR(255),
    taxonomy_family VARCHAR(255),
    taxonomy_subfamily VARCHAR(255),
    taxonomy_genus VARCHAR(255),
    taxonomy_subgenus VARCHAR(255),
    taxonomy_species VARCHAR(255),
    strata TEXT,
    description TEXT
);

